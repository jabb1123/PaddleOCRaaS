# PaddleOCRaaS Configuration
# Copy to .env and modify as needed

# Server configuration
PADDLEOCR_HOST=0.0.0.0
PADDLEOCR_PORT=8000

# Docker Compose port mapping (external:internal)
# Change this to expose the service on a different port
COMPOSE_PORT=8000
